import { StyledBaseDirective } from './styled-base.directive';

describe('StyledBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledBaseDirective();
    expect(directive).toBeTruthy();
  });
});
